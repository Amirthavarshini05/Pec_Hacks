name: vidhya
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Icons
  cupertino_icons: ^1.0.8

  # State Management & Storage
  shared_preferences: ^2.2.2
  
  # HTTP & API
  http: ^1.2.0
  
  # Supabase
  supabase_flutter: ^2.5.0
  
  # Charts
  fl_chart: ^0.68.0
  
  # UI & Utilities
  intl: ^0.19.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Assets - Comment out until you create the folders
  # assets:
  #   - assets/images/
  #   - assets/icons/